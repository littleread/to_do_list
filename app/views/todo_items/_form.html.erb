<%= form_for(todo_item) do |f| %>
  <% if todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
      <% todo_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <table class="table table-striped table-bordered table-condensed">
    <thead>
      <tr>
        <th><%= f.label :description %></th>
        <th><%= f.label :due_date %></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th><%= f.text_field :description %></th>
        <th><%= f.datetime_select :due_date %></th>
      </tr>
    </tbody>
  </div>
</table>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>

